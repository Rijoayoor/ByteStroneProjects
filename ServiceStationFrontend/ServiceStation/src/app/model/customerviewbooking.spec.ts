import { Customerviewbooking } from './customerviewbooking';

describe('Customerviewbooking', () => {
  it('should create an instance', () => {
    expect(new Customerviewbooking()).toBeTruthy();
  });
});
